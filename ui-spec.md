# UI 명세서 · 식물집사

이 문서는 화면 구현의 기준이다. 화면을 만들 때 항상 먼저 읽는다.

---

## 0. 공통 규칙

### 레이아웃
- **앱이 아니라 웹사이트다.** 하단 탭바를 만들지 않는다.
- 상단 네비게이션이 모든 화면에 고정으로 붙는다 (`components/layout/SiteHeader.tsx`).
- 페이지 공통 여백과 최대 너비는 `PageShell` 이 담당한다.
  기본 `max-w-6xl`, 좌우 `px-5`(`md:px-8`), 상하 `py-10`(`md:py-14`).
  글이 중심인 화면은 `<PageShell className="max-w-2xl">` 처럼 좁힌다.
- **데스크톱에서 가로를 쓴다.** 모바일에서는 같은 구조가 1단으로 접힌다.

| 화면 | 데스크톱 | 모바일 |
|---|---|---|
| 홈 | 2단 — 좌 텍스트 / 우 업로드 영역 | 1단 |
| 판별 결과 | 2단 — 좌 사진 / 우 정보·액션 | 1단 |
| 내 식물 | 3열 그리드 (`lg`), 2열 (`sm`) | 1열 |
| 식물 상세 | 2단 — 좌 사진·상태 / 우 관리·기록 | 1단 |

### 색 — 종이 / 우드 톤
`app/globals.css` 의 `:root` 에 CSS 변수로 정의하고 `@theme inline` 으로
Tailwind 에 연결한다. Tailwind v4 라 `tailwind.config` 파일은 없다.
**컴포넌트에 hex 를 직접 쓰지 않는다.**

| 용도 | 토큰 | 유틸리티 | 값 |
|---|---|---|---|
| 페이지 바탕 | `--oat` | `bg-oat` | `#E3DAC6` |
| 콘텐츠 종이색 | `--sheet` | `bg-sheet` | `#FBF8F1` |
| 종이 음영 | `--sheet-2` | `bg-sheet-2` | `#F4EFE3` |
| 프레임, 강한 구분선 | `--wood` | `border-wood` | `#6B5741` |
| 일반 테두리 | `--rule` | `border-rule` | `#D5CCB8` |
| 본문 | `--ink` | `text-ink` | `#2B2A25` |
| 보조 | `--ink-70` | `text-ink-70` | `#5C584E` |
| 흐린 글자 | `--ink-45` | `text-ink-45` | `#918B7D` |
| 액션, 정상 상태 | `--leaf` | `bg-leaf` `text-leaf` | `#2F5D3A` |
| 초록 배경 | `--leaf-50` | `bg-leaf-50` | `#E4EBE0` |
| 경고 | `--clay` | `text-clay` | `#8A5A38` |
| 경고 배경 | `--clay-50` | `bg-clay-50` | `#F0E4D8` |

**초록은 액션과 정상 상태에만 쓴다.** 배경을 초록으로 덮지 않는다.
경고는 빨강 대신 흙빛(`clay`)을 쓴다.

### 타이포
제목은 **Gowun Batang**(명조), 본문과 UI 는 **IBM Plex Sans KR**.
`next/font` 로 불러 `font-serif` / `font-sans` 에 연결한다.

| 용도 | 방법 |
|---|---|
| 화면 제목 | `<SectionTitle as="h1">` — `font-serif text-3xl font-bold tracking-tight` |
| 섹션 제목 | `<SectionTitle as="h2">` — `font-serif text-lg font-bold` |
| 본문 | `text-sm leading-relaxed text-ink` |
| 보조 | `text-xs text-ink-45` |

### 공통 컴포넌트
**같은 마크업이 두 화면에 나오면 컴포넌트로 뽑는다.**
페이지 파일에는 데이터 흐름과 조합만 남기고 스타일은 컴포넌트 안에 둔다.
새 라이브러리를 추가하지 않는다. Tailwind 와 기존 의존성만 쓴다.

`components/layout/` — 사이트 골격

| 파일 | 역할 |
|---|---|
| `SiteHeader` | 상단 네비 + 계정 드롭다운 |
| `AccountMenu` | 계정 버튼과 드롭다운 (바깥 클릭·Esc 로 닫힘) |
| `SiteFooter` | 하단 |
| `PageShell` | 페이지 공통 여백과 최대 너비 |

`components/ui/` — primitive

| 파일 | 요점 |
|---|---|
| `Button` | `variant: primary / ghost / quiet`, `size: sm / md`, `href` 주면 링크 |
| `Card` | `href`, `hover` |
| `Panel` | 상세 페이지 구획 박스, `title` |
| `InfoRow` | 라벨 / 값 한 줄 |
| `Badge` | `tone: leaf / clay / neutral` |
| `Toggle` | `checked`, `onChange`, `label` |
| `Tabs` | 세그먼트 탭 |
| `Timeline` | 기록 타임라인 |
| `StepList` | 번호 붙은 순서 목록 |
| `PhotoFrame` | 사진 + 없을 때 자리표시자 |
| `EmptyState` | 제목 + 설명 + 액션 |
| `Loading` | 해당 영역에만 표시 |
| `ErrorMessage` | 흙빛 오류 안내 |
| `SectionTitle` | 명조 제목 |

### 하지 말 것
- 그라데이션 배경
- 그림자 남발 (`shadow-lg` 이상 금지)
- 이모지를 UI 요소로 사용
- 로딩 중 화면 전체를 가리는 스피너 (해당 영역에만 표시)
- 확신도를 퍼센트로 표기 (예: "92% 일치")

---

## 1. 화면 목록

| ID | 경로 | 화면명 |
|---|---|---|
| S-01 | `/` | 홈 |
| S-02 | `/result` | 판별 결과 |
| S-03 | `/diagnose` | 증상 진단 문답 |
| S-04 | `/diagnose/result` | 진단 결과 |
| S-05 | `/plants` | 내 식물 목록 |
| S-06 | `/plants/[id]` | 식물 상세 |
| S-07 | `/plants/[id]/guide` | 관리 가이드 |
| S-08 | `/account` | 계정 설정 |
| S-09 | `/guide` | 관리 가이드 목록 |

### 상단 네비게이션
모든 화면에 `sticky top-0` 으로 붙는다. **하단 탭바는 만들지 않는다.**

- 좌측 로고 `식물집사` (`Leaf` 아이콘 + 명조) → `/`
- 중앙 메뉴 `홈` → `/` · `내 식물` → `/plants` · `진단` → `/diagnose` · `가이드` → `/guide`
- 우측 계정 버튼 → 드롭다운 → `계정 설정` → `/account`

활성 메뉴만 `bg-sheet text-ink`, 나머지는 `text-ink-70`.
아이콘은 `lucide-react` 를 쓴다.

---

## S-01 홈

**목적** 사용자가 고민 없이 사진 업로드로 이어지게 한다.

### 구성
1. 인사말 `안녕하세요` (xs, `text-ink-45`)
2. 제목 `어떤 식물이 궁금하세요?` (2줄)
3. 한 줄 설명 (sm, `text-ink-70`)
4. **업로드 영역** — 우측 단 전체를 차지한다
   - 드래그앤드롭 영역 `border-2 border-dashed border-rule bg-sheet`
   - 드래그 중에는 `border-leaf bg-leaf-50`
   - 안내 문구 + 주 버튼 `사진 고르기`
     (`<input type="file" accept="image/*">` 를 감싼 label)
   - **카메라 촬영** — `사진 고르기` 옆에 `카메라로 찍기` (`<Button>` 의 `quiet` 스타일)
     (`<input type="file" accept="image/*" capture="environment">` 를 감싼 label,
     모바일에서 후면 카메라가 바로 열린다)
     - 사진 고르기와 같은 핸들러로 이어져 같은 판별 흐름을 탄다
     - **기기를 가리지 않고 항상 보여준다.** 데스크톱 브라우저는 `capture` 를
       무시하고 파일 선택 창을 열므로 숨길 이유가 없다
5. **오늘 할 일 카드** — 좌측 단 아래. 물 줄 식물이 있을 때만 표시
   - 썸네일 + `오늘 물 줄 식물 N` + 식물 이름 나열
   - 클릭 시 `/plants` 이동
   - 없으면 이 영역 자체를 렌더링하지 않는다

### 규칙
- 배너, 광고, 부가 설명 영역을 넣지 않는다.
- 로그인을 요구하지 않는다. 비회원도 업로드와 판별이 가능해야 한다.
- 사진을 놓거나 고르는 즉시 `/api/identify` 호출 후 `/result` 로 이동한다.

---

## S-02 판별 결과

**목적** 판별에서 끝내지 않고 등록으로 넘긴다.

### 구성
1. 좌상단 `← 다시 올리기`
2. (좌측 단) 업로드한 사진 — `<PhotoFrame ratio="photo">`
3. (우측 단) `이 사진과 가장 비슷해요` (xs, `text-ink-45`)
4. **종명** — `<SectionTitle as="h1">` + 학명 (sm, `text-ink-70`, italic)
5. 난이도 배지 — `<Badge tone="leaf">`
6. 정보 행 4~5개: 원산지 / 빛 / 물 / 습도 / 분갈이 (`Panel` + `InfoRow`)
7. **버튼 영역** — 탭바가 없으므로 화면에 고정하지 않는다
   - 주 버튼 `내 식물로 등록`
   - 보조 버튼 `관리법 먼저 보기`
   - 데스크톱은 가로로 나란히, 모바일은 세로로 쌓는다
8. 안내문 `다른 식물인가요? 사진을 다시 올리면 더 정확해집니다.`

### 판별 실패 시 (`confident: false`)
- 종명 자리에 `판별하지 못했어요`
- 정보 행 숨김
- `다시 올리기` 버튼만 노출

### 규칙
- **결과는 주소에 담는다.** `/result?id=xxx` 로 `identifications` 의 한 줄을 조회한다.
  다른 화면에 갔다 와도, 새로고침해도 결과가 남아야 한다.
- 판별 실패나 세션이 없어 기록을 남기지 못했을 때만 `sessionStorage` 로 넘긴다.
- 등록은 `identificationId` 만 보낸다. 사진과 종 정보는 서버가 그 기록에서 복사한다.

---

## S-03 증상 진단 문답

**목적** 사진만으로 단정하지 않고 되물어 원인을 좁힌다.
**이 화면이 프로젝트의 핵심 차별점이다.**

글이 중심이므로 `<PageShell className="max-w-2xl">` 로 좁힌다.

### 구성
1. 좌상단 `← 나가기`
2. **진행률 바** — `h-1 bg-rule`, 채움 `bg-leaf`
3. `질문 N / 3` (xs)
4. **질문** — `<SectionTitle as="h1">`, 2줄 이내
5. **왜 묻는지 설명** (sm, `text-ink-70`) — 한 줄
6. **선택지 3~4개** — 세로 스택
   - `w-full text-left border border-rule bg-sheet rounded-md px-4 py-3.5`
   - hover 시 `border-leaf bg-leaf-50`
7. 마지막 선택지는 항상 `잘 기억나지 않아요` 계열을 포함한다

### 규칙
- 질문은 **최대 3개**. 그 이상 묻지 않는다.
- 채팅 UI 로 만들지 않는다. 한 화면에 질문 하나만 표시한다.
- 선택 즉시 다음 질문으로 넘어간다. 확인 버튼을 두지 않는다.
- 이전 답변은 상태로 누적해 다음 요청에 함께 보낸다.

---

## S-04 진단 결과

**목적** 결론보다 근거를 먼저 보여준다.

### 구성
1. **원인 배지** — `<Badge tone="clay">` (예: `과습으로 보여요`)
2. **조치 제목** — `<SectionTitle as="h1">`, 2줄 이내 (예: `물을 잠시 멈춰주세요`)
3. `이렇게 판단했어요` 섹션
   - 근거 3줄, `·` 로 시작하는 리스트
   - 사진에서 본 것과 답변에서 확인한 것을 섞어 쓴다
4. `지금 할 일` 섹션
   - 체크박스 형태 3개
   - 읽는 정보가 아니라 실행 목록으로 보이게 한다
5. 주 버튼 `기록에 저장`
6. **면책 문구** (xs, `text-ink-45`, 좌측 `border-rule`)
   `참고용 안내입니다. 상태가 심하면 가까운 화훼농원에 문의하세요.`

### 규칙
- 저장 시 해당 식물의 `water_interval` 을 진단 결과에 맞게 갱신한다.
- 진단 이력을 `diagnoses` 테이블에 저장한다.

---

## S-05 내 식물 목록

**목적** 목록만 봐도 오늘 할 일이 보여야 한다.

### 구성
1. 제목 `내 식물` + `N개 · 오늘 물 줄 식물 M` (xs)
2. **식물 카드 그리드** — `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`
   - 위: 사진 `<PhotoFrame ratio="photo">`
   - 아래 좌: 별칭(명조) / `위치 · 등록 N일째` (xs, `text-ink-45`)
   - 아래 우: **상태 배지** — `오늘` `D-4` 등
     - 오늘 또는 지남 → `<Badge tone="clay">`
     - 여유 있음 → `<Badge tone="leaf">`
3. 제목 우측 보조 버튼 `새 식물 추가` → `/`
4. **최근 본 식물** — 목록 맨 아래. 등록한 식물과 섞이지 않게 구분한다
   - 위에 구분선(`border-t border-rule`) + 섹션 제목 `최근 본 식물`
   - 가로 스크롤 카드 (`<Card>` + `<PhotoFrame ratio="square">` + 종명)
   - `identifications` 중 **아직 등록하지 않은 것만** 표시한다
   - 카드를 누르면 `/result?id=xxx` 로 가서 다시 등록할 수 있다
   - 항목이 없으면 이 영역 자체를 렌더링하지 않는다
   - **홈에는 넣지 않는다.** 홈은 촬영에 집중시킨다

### 규칙
- **정렬은 급한 순.** 등록순이 아니다.
- **빈 화면을 반드시 구현한다.** `<EmptyState>` 를 쓴다.
  - 제목 + 안내 문구 한 줄 + 업로드 버튼 (`<PhotoUpload variant="button">`)
  - 심사 시 새 계정으로 처음 보는 화면이므로 비워두지 않는다.

---

## S-06 식물 상세

**목적** 사용자는 대부분 "물 줬어요" 하나를 누르러 들어온다.

### 구성
데스크톱에서 2단으로 나눈다. 모바일에서는 좌 → 우 순서로 1단이 된다.

0. 좌상단 `← 내 식물`

**좌측 단 — 지금 어떤 상태인가**
1. 등록 사진 — `<PhotoFrame ratio="video">`
2. 별칭 (`<SectionTitle as="h1">`) + `위치 · N일째 함께` (xs, `text-ink-45`)
3. **다음 물주기 박스** — `<Panel className="text-center">`
   - `다음 물주기` (xs)
   - **`오늘`** 또는 `3일 뒤` (명조 3xl, `text-leaf`)
4. **종 정보 패널** — `<Panel title={종명}>` + `<InfoRow>`
   - 학명(italic) / 난이도 / 원산지 / 빛 / 물 / 습도 / 분갈이
   - 등록할 때 판별 결과에서 `plants` 로 복사해 둔 값이다.
     판별 기록(`identifications`)이 지워져도 남아야 한다
   - 값이 없는 줄은 건너뛰고, 전부 없으면 패널을 렌더링하지 않는다
     (컬럼이 생기기 전에 등록한 식물)
5. **센서 패널 자리 (9주차 추가 예정)**
   - 토양 수분 · 조도 · 온습도를 이 자리에 붙인다
   - 그때까지는 렌더링하지 않는다. 자리만 비워둔다

**우측 단 — 무엇을 할 수 있는가**
6. `관리` 패널
   - 주 버튼 `물 줬어요`
   - 보조 버튼 `잎이 이상해요 · 진단하기` → `/diagnose?plantId={id}`
   - 보조 버튼 `관리 가이드 보기` → `/plants/[id]/guide`
7. `기록` 패널 — `<Timeline>`
   - 좌측 세로선 + 점, 최신 항목만 `bg-leaf` 점
   - 각 항목: 날짜(xs, `text-ink-45`) / 내용(sm)
   - 최근 5건만 표시, 나머지는 접기

### 규칙
- `물 줬어요` 는 **한 번의 클릭으로 끝난다.** 날짜 입력창을 띄우지 않는다.
- 누르면 `last_watered` 갱신 + `care_logs` 추가가 동시에 일어난다.

#### 과습 막기 — 이 서비스의 핵심
아무 저항 없이 물주기가 기록되면 서비스의 목적과 어긋난다.

- **같은 날 두 번 기록하지 않는다.**
  오늘 기록이 있으면 버튼을 `오늘 물 줬어요 ✓` 로 바꾸고 비활성화한다.
  아래에 `다음 물주기는 N일 뒤예요`, 그 아래에 작게 `기록 취소`.
- **예정일까지 3일 이상 남았으면 바로 기록하지 않는다.** 확인 단계를 둔다.
  - 문구: `아직 물 줄 때가 아니에요. 지금 주면 과습으로 뿌리가 상할 수 있습니다.
    흙이 정말 말랐나요?` (`text-clay`)
  - 버튼: `흙이 말랐어요, 기록할게요` / `취소`
  - 그래도 기록하면 저장하되 로그 문구를 `예정보다 N일 이른 물주기` 로 남긴다
- **이른 물주기가 3회 쌓이면** 상세 상단에 안내를 띄운다.
  `최근 물을 자주 주고 있어요. 과습이 의심되면 진단해 보세요` + 진단 링크

#### 기록 표시
- 날짜에 시각까지 적는다 (`9월 16일 14:20`). 같은 날 여러 건을 구분해야 한다.
- 종류별로 문구가 다르다: 물주기 / 분갈이 / 비료 / 진단 / 이른 물주기

---

## S-07 관리 가이드

**목적** 검색으로 나오는 일반론과 달라야 한다.

### 구성
1. 좌상단 `← 돌아가기`
2. 제목 `{식물명} 관리법`
3. **탭 4개** — 물주기 / 빛 / 분갈이 / 비료 (`<Tabs>`)
   - 활성 탭 `bg-wood text-sheet`
4. `언제 하나요` 섹션 — 시기 판단 기준
5. `순서` 섹션 — 번호가 붙는 유일한 곳 (`<StepList>`)
   - 원형 번호 배지 `bg-leaf-50 text-leaf`
6. 주의 문구 (xs, 좌측 `border-rule`)

### 규칙
- 프롬프트에 종명뿐 아니라 **등록된 위치, 등록 기간**을 함께 전달한다.
- 같은 종이라도 사용자 환경에 따라 답이 달라져야 한다.
- 생성된 가이드는 캐싱한다. 매번 API 를 호출하지 않는다.
  `guides` 테이블에 **식물 × 탭** 단위로 한 줄씩 저장하고 다시 쓴다.
- 탭을 눌렀을 때만 그 탭을 만든다. 4개를 미리 만들지 않는다.
- 만드는 동안 화면 전체를 가리지 않는다. 내용 자리에만 `<Loading>` 을 둔다.

---

## S-08 계정 설정

**목적** 없으면 미완성으로 보이는 화면. 기능은 적어도 된다.
상단 네비의 계정 드롭다운에서 들어온다.

### 구성
1. 제목 `계정 설정`
2. 요약 카드 2개 (`grid-cols-2`) — 내 식물 수 / 진단 횟수
3. `알림` 패널 — 물주기 알림, 분갈이 시기 알림 (`<Toggle>`)
4. `계정` 패널 — 이용 상태(게스트), 이메일 연결 여부, `이메일 연결 (준비 중)` 버튼
   - 로그아웃은 넣지 않는다. 익명 계정은 로그아웃하면 다시 들어올 수 없어 등록한 식물을 잃는다.

### 규칙
- **우선순위가 가장 낮다.** 다른 화면이 끝난 뒤에 만든다.

---

## S-09 관리 가이드 목록

**목적** 상단 네비의 `가이드` 가 갈 곳.
가이드는 식물마다 다르므로 어떤 식물의 가이드를 볼지 먼저 고르게 한다.

### 구성
1. 제목 `관리 가이드` + 한 줄 설명
2. 등록한 식물 카드 그리드 — 누르면 `/plants/[id]/guide`
3. 식물이 없으면 `<EmptyState>` + 업로드 버튼

### 규칙
- S-07 의 내용을 여기서 다시 보여주지 않는다. 고르는 역할만 한다.

---

## 2. 구현 순서

1. `components/ui` primitive → `components/layout` (`SiteHeader` 등) — 모든 화면에 공통
2. S-01 홈
3. S-02 판별 결과
4. S-05 내 식물 목록 (빈 화면 포함)
5. S-06 식물 상세
6. S-03 → S-04 진단
7. S-07 관리 가이드
8. S-09 관리 가이드 목록
9. S-08 계정 설정

**한 화면을 끝까지 완성하고 다음으로 넘어간다.**
여러 화면을 동시에 반쯤 만들지 않는다.

---

## 3. 기술 제약

- Gemini API 호출은 **반드시 `app/api/` 안에서** 한다. 클라이언트에서 직접 호출하면 키가 노출된다.
- 프롬프트는 `lib/prompts.ts` 에만 둔다. 화면 코드에 문자열로 넣지 않는다.
- 사진 업로드 전 클라이언트에서 리사이즈한다 (긴 변 1024px). API 비용과 응답 속도에 직접 영향을 준다.
- 상태 관리 라이브러리를 추가하지 않는다. `useState` 로 충분하다.
- UI 라이브러리를 추가하지 않는다. Tailwind 와 `components/ui` 로 만든다.
- 색은 토큰으로만 쓴다. 컴포넌트에 hex 를 직접 넣지 않는다.
- 차트 라이브러리를 추가하지 않는다. 필요하면 CSS 로 그린다.