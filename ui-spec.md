# UI 명세서 · 식물집사

이 문서는 화면 구현의 기준이다. 화면을 만들 때 항상 먼저 읽는다.

---

## 0. 공통 규칙

### 레이아웃
- **모바일 우선.** 390px 기준으로 설계하고, PC 에서는 가운데 정렬만 한다.
- 최대 너비 `max-w-md`, 좌우 여백 `px-5`
- 하단 탭바가 항상 떠 있으므로 본문 하단에 `pb-20` 여백을 준다.

### 색
Tailwind 기본 팔레트만 쓴다. 커스텀 색을 정의하지 않는다.

| 용도 | 클래스 |
|---|---|
| 주 색 (버튼, 강조) | `emerald-700` |
| 주 색 배경 | `emerald-50` |
| 본문 텍스트 | `neutral-900` |
| 보조 텍스트 | `neutral-500` |
| 흐린 텍스트 | `neutral-400` |
| 테두리 | `neutral-200` |
| 경고 / 주의 | `amber-700`, 배경 `amber-50` |

**초록은 액션과 강조에만 쓴다.** 배경을 초록으로 덮지 않는다.

### 타이포
| 용도 | 클래스 |
|---|---|
| 화면 제목 | `text-2xl font-semibold tracking-tight` |
| 섹션 제목 | `text-base font-semibold` |
| 본문 | `text-sm leading-relaxed` |
| 보조 | `text-xs text-neutral-500` |

### 공통 컴포넌트
- 버튼: `rounded-lg py-3.5 text-sm font-semibold`
  - 주 버튼 `bg-emerald-700 text-white`
  - 보조 버튼 `border border-neutral-200 text-neutral-600`
- 카드: `rounded-xl border border-neutral-200 p-4`
- 정보 행: 좌측 라벨 `text-neutral-400`, 우측 값 `font-medium`, 하단 `border-b border-neutral-100`

### 하지 말 것
- 그라데이션 배경
- 그림자 남발 (`shadow-lg` 이상 금지)
- 이모지를 UI 요소로 사용
- 로딩 중 화면 전체를 가리는 스피너 (해당 영역에만 표시)
- 확신도를 퍼센트로 표기 (예: "92% 일치")

---

## 1. 화면 목록

| ID | 경로 | 화면명 |
|---|---|---|
| S-01 | `/` | 홈 |
| S-02 | `/result` | 판별 결과 |
| S-03 | `/diagnose` | 증상 진단 문답 |
| S-04 | `/diagnose/result` | 진단 결과 |
| S-05 | `/plants` | 내 식물 목록 |
| S-06 | `/plants/[id]` | 식물 상세 |
| S-07 | `/plants/[id]/guide` | 관리 가이드 |
| S-08 | `/mypage` | 마이페이지 |

### 하단 탭바 (3개)
`홈` → `/` · `내 식물` → `/plants` · `마이` → `/mypage`

아이콘은 `lucide-react` 사용. 홈은 `Camera`, 내 식물은 `Leaf`, 마이는 `User`.
활성 탭만 `emerald-700`, 나머지는 `neutral-400`.

---

## S-01 홈

**목적** 사용자가 고민 없이 촬영으로 이어지게 한다.

### 구성
1. 인사말 `안녕하세요` (xs, neutral-400)
2. 제목 `어떤 식물이 궁금하세요?` (2줄)
3. **촬영 버튼** — 화면에서 가장 큰 요소
   - 원형, 150×150px, `border-2 border-emerald-700 bg-emerald-50`
   - 안에 카메라 아이콘 + `식물 사진 찍기`
   - `<input type="file" accept="image/*" capture="environment">` 를 감싼 label
4. 보조 버튼 `앨범에서 고르기`
5. **오늘 할 일 카드** — 물 줄 식물이 있을 때만 표시
   - 썸네일 + `오늘 물 줄 식물 N` + 식물 이름 나열
   - 클릭 시 `/plants` 이동
   - 없으면 이 영역 자체를 렌더링하지 않는다

### 규칙
- 배너, 광고, 부가 설명 영역을 넣지 않는다.
- 로그인을 요구하지 않는다. 비회원도 촬영과 판별이 가능해야 한다.
- 사진 선택 즉시 `/api/identify` 호출 후 `/result` 로 이동한다.

---

## S-02 판별 결과

**목적** 판별에서 끝내지 않고 등록으로 넘긴다.

### 구성
1. 좌상단 `← 다시 찍기`
2. 업로드한 사진 (`aspect-[4/3] object-cover rounded-lg`)
3. `이 사진과 가장 비슷해요` (xs, neutral-400)
4. **종명** (2xl, semibold) + 학명 (sm, neutral-500)
5. 난이도 배지 — `emerald-50` 배경의 작은 칩
6. 정보 행 4~5개: 원산지 / 빛 / 물 / 습도 / 분갈이
7. **하단 고정 버튼 영역** (`sticky bottom-16 bg-white pt-3`)
   - 주 버튼 `내 식물로 등록`
   - 보조 버튼 `관리법 먼저 보기`
8. 하단 안내문 `다른 식물인가요? 사진을 다시 찍으면 더 정확해집니다.`

### 판별 실패 시 (`confident: false`)
- 종명 자리에 `판별하지 못했어요`
- 정보 행 숨김
- 재촬영 버튼만 노출

---

## S-03 증상 진단 문답

**목적** 사진만으로 단정하지 않고 되물어 원인을 좁힌다.
**이 화면이 프로젝트의 핵심 차별점이다.**

### 구성
1. 좌상단 `← 나가기`
2. **진행률 바** — `h-1 bg-neutral-200`, 채움 `bg-emerald-700`
3. `질문 N / 3` (xs)
4. **질문** (2xl, 2줄 이내)
5. **왜 묻는지 설명** (sm, neutral-500) — 한 줄
6. **선택지 3~4개** — 세로 스택
   - `w-full text-left border border-neutral-200 rounded-lg px-4 py-3.5`
   - hover 시 `border-emerald-700 bg-emerald-50`
7. 마지막 선택지는 항상 `잘 기억나지 않아요` 계열을 포함한다

### 규칙
- 질문은 **최대 3개**. 그 이상 묻지 않는다.
- 채팅 UI 로 만들지 않는다. 한 화면에 질문 하나만 표시한다.
- 선택 즉시 다음 질문으로 넘어간다. 확인 버튼을 두지 않는다.
- 이전 답변은 상태로 누적해 다음 요청에 함께 보낸다.

---

## S-04 진단 결과

**목적** 결론보다 근거를 먼저 보여준다.

### 구성
1. **원인 배지** — `amber-50` 배경, `amber-700` 텍스트 (예: `과습으로 보여요`)
2. **조치 제목** (2xl, 2줄) — 예: `물을 잠시 멈춰주세요`
3. `이렇게 판단했어요` 섹션
   - 근거 3줄, `·` 로 시작하는 리스트
   - 사진에서 본 것과 답변에서 확인한 것을 섞어 쓴다
4. `지금 할 일` 섹션
   - 체크박스 형태 3개
   - 읽는 정보가 아니라 실행 목록으로 보이게 한다
5. 주 버튼 `기록에 저장`
6. **면책 문구** (xs, neutral-400, 좌측 border)
   `참고용 안내입니다. 상태가 심하면 가까운 화훼농원에 문의하세요.`

### 규칙
- 저장 시 해당 식물의 `water_interval` 을 진단 결과에 맞게 갱신한다.
- 진단 이력을 `diagnoses` 테이블에 저장한다.

---

## S-05 내 식물 목록

**목적** 목록만 봐도 오늘 할 일이 보여야 한다.

### 구성
1. 제목 `내 식물` + `N개 · 오늘 물 줄 식물 M` (xs)
2. **식물 카드** (세로 스택)
   - 좌: 썸네일 52×52 `rounded-lg`
   - 중: 별칭(semibold) / `위치 · 등록 N일째` (xs, neutral-400)
   - 우: **상태 배지** — `오늘` `D-4` 등
     - 오늘 또는 지남 → `amber-50 / amber-700`
     - 여유 있음 → `emerald-50 / emerald-700`
3. 하단 보조 버튼 `+ 새 식물 추가` → `/`

### 규칙
- **정렬은 급한 순.** 등록순이 아니다.
- **빈 화면을 반드시 구현한다.**
  - 안내 문구 한 줄 + 촬영 버튼
  - 심사 시 새 계정으로 처음 보는 화면이므로 비워두지 않는다.

---

## S-06 식물 상세

**목적** 사용자는 대부분 "물 줬어요" 하나를 누르러 들어온다.

### 구성
1. 좌상단 `← 내 식물`
2. 등록 사진 (`aspect-video object-cover`)
3. 별칭 (2xl) + `위치 · N일째 함께` (xs)
4. **다음 물주기 박스** (`border rounded-xl p-4 text-center`)
   - `다음 물주기` (xs)
   - **`오늘`** 또는 `3일 뒤` (2xl, emerald-700)
   - 주 버튼 `물 줬어요`
5. 보조 버튼 `잎이 이상해요 · 진단하기` → `/diagnose`
6. `기록` 섹션 — 타임라인
   - 좌측 세로선 + 점, 최신 항목만 `emerald-700` 점
   - 각 항목: 날짜(xs, neutral-400) / 내용(sm)
   - 최근 5건만 표시, 나머지는 접기

### 규칙
- `물 줬어요` 는 **한 번의 탭으로 끝난다.** 날짜 입력창을 띄우지 않는다.
- 누르면 `last_watered` 갱신 + `care_logs` 추가가 동시에 일어난다.

---

## S-07 관리 가이드

**목적** 검색으로 나오는 일반론과 달라야 한다.

### 구성
1. 좌상단 `← 돌아가기`
2. 제목 `{식물명} 관리법`
3. **탭 4개** — 물주기 / 빛 / 분갈이 / 비료
   - `grid-cols-4`, 활성 탭 `bg-neutral-900 text-white`
4. `언제 하나요` 섹션 — 시기 판단 기준
5. `순서` 섹션 — 번호가 붙는 유일한 곳
   - 원형 번호 배지 `bg-emerald-50 text-emerald-700`
6. 주의 문구 (xs, 좌측 border)

### 규칙
- 프롬프트에 종명뿐 아니라 **등록된 위치, 등록 기간**을 함께 전달한다.
- 같은 종이라도 사용자 환경에 따라 답이 달라져야 한다.
- 생성된 가이드는 캐싱한다. 매번 API 를 호출하지 않는다.

---

## S-08 마이페이지

**목적** 없으면 미완성으로 보이는 화면. 기능은 적어도 된다.

### 구성
1. 제목 `마이페이지`
2. 요약 카드 2개 (`grid-cols-2`) — 내 식물 수 / 진단 횟수
3. `알림` 섹션 — 물주기 알림, 분갈이 시기 알림 (토글)
4. `계정` 섹션 — 이메일, 로그아웃

### 규칙
- **우선순위가 가장 낮다.** 다른 화면이 끝난 뒤에 만든다.

---

## 2. 구현 순서

1. `components/TabBar.tsx` — 모든 화면에 공통
2. S-01 홈
3. S-02 판별 결과
4. S-05 내 식물 목록 (빈 화면 포함)
5. S-06 식물 상세
6. S-03 → S-04 진단
7. S-07 관리 가이드
8. S-08 마이페이지

**한 화면을 끝까지 완성하고 다음으로 넘어간다.**
여러 화면을 동시에 반쯤 만들지 않는다.

---

## 3. 기술 제약

- Gemini API 호출은 **반드시 `app/api/` 안에서** 한다. 클라이언트에서 직접 호출하면 키가 노출된다.
- 프롬프트는 `lib/prompts.ts` 에만 둔다. 화면 코드에 문자열로 넣지 않는다.
- 사진 업로드 전 클라이언트에서 리사이즈한다 (긴 변 1024px). API 비용과 응답 속도에 직접 영향을 준다.
- 상태 관리 라이브러리를 추가하지 않는다. `useState` 로 충분하다.
- 차트 라이브러리를 추가하지 않는다. 필요하면 CSS 로 그린다.